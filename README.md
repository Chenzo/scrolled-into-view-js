# scrolled-into-view-js
Simple JavaScript code to detect if an Element has scrolled into the view port of a browser and then add a css class to that element

the only file you really need is the JS file.

Example here:
